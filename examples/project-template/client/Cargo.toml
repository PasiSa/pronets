[package]
name = "client"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
slint = "1.17"
tokio = { version = "1.52", features = ["io-util", "macros", "net", "rt", "sync"] }

[build-dependencies]
slint-build = "1.17"
